<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Minecraft Teamliste</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #0e1117;
      color: #e4e6eb;
      padding: 30px;
    }
    h1 {
      color: #00ff88;
    }
    .task {
      margin-bottom: 10px;
    }
    label {
      cursor: pointer;
    }
    img {
      max-width: 300px;
      border-radius: 10px;
      margin-top: 10px;
      box-shadow: 0 0 10px #00ff88;
    }
  </style>
</head>
<body>
  <h1>Minecraft Team-Checkliste</h1>

  <div class="task">
    <input type="checkbox" id="spawn" />
    <label for="spawn">Spawn gebaut</label><br />
    <img src="images/spawn.jpg" alt="Spawn-Bild" />
  </div>

  <div class="task">
    <input type="checkbox" id="farm" />
    <label for="farm">Farm fertiggestellt</label><br />
    <img src="images/farm.jpg" alt="Farm-Bild" />
  </div>

  <div class="task">
    <input type="checkbox" id="shop" />
    <label for="shop">Shop eingerichtet</label><br />
    <img src="images/shop.jpg" alt="Shop-Bild" />
  </div>

  <script>
    // Checkbox-Zustände speichern
    const boxes = document.querySelectorAll('input[type="checkbox"]');
    boxes.forEach(box => {
      const id = box.id;
      box.checked = localStorage.getItem(id) === 'true';
      box.addEventListener('change', () => {
        localStorage.setItem(id, box.checked);
      });
    });
  </script>
</body>
</html>
